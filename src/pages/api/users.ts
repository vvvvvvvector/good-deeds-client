export const getMyEmailAndUsername = (token: string) => {};

export const deleteMyProfile = (token: string) => {};

export const updateMyProfile = (
  dto: {
    email: string;
    username: string;
    password: string;
  },
  token: string
) => {};
